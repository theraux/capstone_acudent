const messageCount = 2;
const notificationCount = 5;

document.querySelector('.fa-envelope + .badge').textContent = messageCount;
document.querySelector('.fa-bell + .badge').textContent = notificationCount;
